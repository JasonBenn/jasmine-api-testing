/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var NetworkRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return NetworkRouter;
});